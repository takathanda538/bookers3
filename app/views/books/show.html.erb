<div class= "container">
  <div class="row">
    <div class= "col-md-3">
      <%= render 'users/info', user: @user %>
      <%= render 'book', book: @booknew %>
    </div>
    <div class= "col-md-8 offset-md-1">
      <h2>Book detail</h2>
      <table class= "table">
        <tbody>
          <tr>
            <td>
              <%= link_to user_path(@user.id) do %>
              <%= image_tag @user.get_profile_image(100,100) %><br>
              <%= @user.name %>
              <% end %>
            </td>
            <td>
              <%= link_to book_path(@book.id) do %>
              <%= @book.title %>
              <% end %>
            </td>
            <td>
              <%= @book.body %>
            </td>
            <td>
              <div id="favorite_btn_<%= @book.id %>">
              <%= render 'favorites/favorite-btn', book: @book %>
              </end>
            </td>
            <td>
              コメント数:<%= @book.book_comments.count %>
            </td>
            <td>
              👀数: <%= @book.view_counts.count %>
            </td>
            <% if @book.user == current_user %>
              <td><%= link_to "Edit", edit_book_path(@book.id), class:"btn btn-sm btn-success" %></td>
              <td><%= link_to "Destory", book_path(@book.id), method: :delete, class:"btn btn-sm btn-danger" %></td>
            <% end %>
          </tr>
        </tbody>
      </table>
      <div class="row">
          <%= render 'book_comments/book_comments_index', book: @book %>
        <div class="col-12">
            <%= render 'book_comments/form', book: @book, book_comment: @book_comment %>
        </div>
      </div>
    </div>
  </div>
</div>